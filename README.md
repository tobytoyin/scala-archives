Use below to create new archive:

```shell
cp -r template/ <archive>
cd <archive>
```

Use below to run script: 
```shell
scala <archive>/src/main/scala/<script>.scala
```

Or use sbt to build, then run:
```shell
sbt run
```